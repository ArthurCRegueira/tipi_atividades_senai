#include <stdio.h>

    int main(){
        // printf("tipo de dado", nomeDaVariavel);
        // printf("%tipo1 %tipo2", var1, var2);
        int num = 11;
        printf("%i\n", num);
        float num2 = 2.4;
        printf("%f\n", num2);
        printf("\n\n");
        /*
            int %i Ou %d
            float / double %F
            char %c
            str (sting) %s

            %u módulo de um número
            %p endereço na memória
            %e ou %E para notação científica
            %% para imprimir um símblo de porcentagem
        */
        int x = 10;
        float ff = 3.10920;
        printf("%i - %f\n", x, ff);
        return 0;
    }